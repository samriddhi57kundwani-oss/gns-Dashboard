{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],

  "github": {
    "silent": false
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
